# GitHub metadata

Suggested description: Renderer-neutral A3UI visual tokens, schemas, and phased graphics specification.

Suggested topics: `design-tokens`, `json-schema`, `graphics`, `a3ui`, `accessibility`.

These are manual GitHub settings; no remote metadata is changed by this repository.